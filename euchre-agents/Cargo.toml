[package]
name = "euchre-agents"
version = "0.1.0"
edition = "2024"

[dependencies]
euchre-interface = { path = "../euchre-interface" }

[dev-dependencies]
euchre-engine = { path = "../euchre-engine" }
