[package]
name = "euchre-agents"
version = "0.1.0"
edition = "2024"

[dependencies]
euchre-interface = { path = "../euchre-interface" }
rand = "0.10.1"

[dev-dependencies]
euchre-engine = { path = "../euchre-engine" }
